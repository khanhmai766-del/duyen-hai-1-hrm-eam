node_modules
.next
out
build
.env
.env*.local
*.log
.DS_Store
.vercel
next-env.d.ts
tsconfig.tsbuildinfo
prisma/dev.db
.pgdata
public/uploads

# Tệp người dùng tải lên & video nền dung lượng lớn (cấp riêng khi triển khai)
# Giữ riêng video nền trang đăng nhập (bg.mp4 — đã ghép bg1–bg3) để deploy cùng code.
public/videos/*
!public/videos/bg.mp4

# Cấu hình Claude theo máy (không chia sẻ)
.claude/settings.local.json

# Artifact tạm từ công cụ chạy thử (Codex): log dev-server, output test, video frames
/codex-*.err
/codex-*.txt
/.codex-video-frames/
/.codex-shdc-*/

# Bản sao lưu do script dedupe thiết bị sinh ra (không đưa vào git)
scripts/data/equipment-dedupe-backup-*.json
